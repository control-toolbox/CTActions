name: Update README with ABOUT.md and INSTALL.md

on:
  workflow_call:
    inputs:
      template_file:
        description: 'Path to README template file'
        required: true
        type: string
      output_file:
        description: 'Path to generated README'
        required: true
        type: string
      package_name:
        description: 'Name of the Julia package for Installation section'
        required: true
        type: string

jobs:
  generate-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write      # needed to push branch
      pull-requests: write # needed to create PR
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Fetch ABOUT.md from central repo (temporary file)
        run: |
          curl -sL "https://raw.githubusercontent.com/control-toolbox/.github/main/ABOUT.md" -o ABOUT.md

      - name: Fetch INSTALL.md from central repo (temporary file)
        run: |
          curl -sL "https://raw.githubusercontent.com/control-toolbox/.github/main/INSTALL.md" -o INSTALL.md

      - name: Generate README with substituted placeholders
        run: |
          PACKAGE="${{ inputs.package_name }}"
          
          # replace {{PACKAGE}} in INSTALL.md
          sed "s/{{PACKAGE}}/$PACKAGE/g" INSTALL.md > INSTALL.tmp.md
          
          # insert ABOUT.md
          sed '/<!-- INCLUDE_ABOUT -->/ {
            r ABOUT.md
            d
          }' "${{ inputs.template_file }}" > README.tmp.md
          
          # insert INSTALL.md
          sed '/<!-- INCLUDE_INSTALL -->/ {
            r INSTALL.tmp.md
            d
          }' README.tmp.md > "${{ inputs.output_file }}"

      - name: Stage README changes only
        run: |
          git add "${{ inputs.output_file }}"

      - name: Commit changes if needed
        run: |
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          else
            git config --local user.name "github-actions[bot]"
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git commit -m "Update README with latest ABOUT.md and INSTALL.md"

      - name: Push changes and create PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="update-readme-$(date +%s)"
          git push origin HEAD:$BRANCH

          gh pr create \
            --title "Update README with latest ABOUT.md and INSTALL.md" \
            --body "This PR updates the README with the latest version of ABOUT.md and INSTALL.md from the control-toolbox/.github repo." \
            --base main \
            --head $BRANCH \
            --assignee ocots || echo "PR already exists or could not be created"

name: Documentation

on:
  workflow_call:
    inputs:
      use_ct_registry:
        required: false
        default: false
        type: boolean
    secrets:
      SSH_KEY:
        required: false
        
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - uses: julia-actions/setup-julia@latest
      
      # Add ct-registry (only if use_registry is true and SSH_KEY is provided)
      - name: Add ct-registry
        if: inputs.use_ct_registry
        uses: julia-actions/add-julia-registry@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          registry: control-toolbox/ct-registry

      # 🧰 Cache des artefacts (binaires des paquets)
      - name: Cache Julia artifacts
        uses: actions/cache@v5
        with:
          path: ~/.julia/artifacts
          key: ${{ runner.os }}-julia-artifacts-${{ hashFiles('docs/Manifest.toml') }}
          restore-keys: |
            ${{ runner.os }}-julia-artifacts-

      # ⚡ Cache du code précompilé (fichiers .ji)
      - name: Cache Julia compiled code
        uses: actions/cache@v5
        with:
          path: ~/.julia/compiled
          key: ${{ runner.os }}-julia-compiled-${{ hashFiles('docs/Manifest.toml') }}
          restore-keys: |
            ${{ runner.os }}-julia-compiled-
            
      - uses: julia-actions/julia-buildpkg@latest
      
      - name: Install dependencies
        run: julia --project=docs/ -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'

      - name: Build and deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
          GKSwstype: 100
        run: |
          julia --project=docs/ -e 'ENV["GKSwstype"]="nul"; include("docs/make.jl")'
          echo "📂 Listing docs/build/assets:"
          ls -R docs/build/assets || true
